#include <stdio.h>
#include <math.h>
int main()
{
    int i,n;
    printf("enter the number upto which you want to print numbers: ");
    scanf("%d",&n);
    for(i=0; i<=n; i++)
    {
      printf("%d ",i);
    }
   return 0;
}
